import Logo from "../Logo/Logo";
import { FooterWrapper } from "./Footer.styled";

function Footer() {
  return (
    <FooterWrapper>
      <Logo />
    </FooterWrapper>
  );
}

export default Footer;
