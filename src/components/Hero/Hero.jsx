import { HeroTitle, HeroWrapper } from "./Hero.styled";

function Hero() {
  return (
    <div>
      <HeroWrapper>
        <HeroTitle>Hero</HeroTitle>
      </HeroWrapper>
    </div>
  );
}

export default Hero;
